<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="MathApp.ItemsPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MathApp"
    xmlns:data="using:MathApp.Data"
    xmlns:common="using:MathApp.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="Mathematics" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="50">
            <TextBlock.Foreground>
                <SolidColorBrush Color="White" Opacity="0.7"/>
            </TextBlock.Foreground>
        </TextBlock>
    </Grid>
    <Page.BottomAppBar>
        <AppBar>
            <StackPanel Orientation="Horizontal">
                <AppBarToggleButton Icon="Rotate" Label="Update"/>
            </StackPanel>
        </AppBar>
    </Page.BottomAppBar>
</common:LayoutAwarePage>
